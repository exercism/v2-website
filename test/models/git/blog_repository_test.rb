require "test_helper"

class Git::BlogRepositoryTest < ActiveSupport::TestCase
  # TODO - This needs implementing but I don't understand
  # the whole git syncing thing enough to do it, so I've
  # smoke tested it locally for now.W
  test "retrieves blog post" do
  end
end
