#track-page
  #nav.lo-nav-bar
    .lo-container
      =link_to "Home", root_path
      =image_tag "nav-divider.png"
      =link_to "Language Tracks", [:tracks]
      =image_tag "nav-divider.png"
      %span #{@track.title} Track

  .header
    .visual-section
      .line
      .lo-container
        =bordered_turquoise_track_icon(@track, class: 'logo')
    .lo-container
      %h1= @track.title
      = link_to "Join the #{@track.title} Track", [:join, @track], method: :post, class: 'pure-button'
      .h1-subtitle= @track.introduction

  =render "tracks/overview"
  =render "tracks/about", button_url: [:join, @track]
  =render "tracks/testimonial", testimonial: @testimonial
  =render "tracks/mentors", mentors: @mentors, track: @track
  =render "tracks/exercises"
  =render "tracks/maintainers", maintainers: @maintainers, track: @track

  .cta-section
    .lo-container
      %h2 Get started with the #{@track.title} track. As with everything on Exercism, it's 100% 
      = link_to "Join the #{@track.title} Track", [:join, @track], method: :post, class: 'pure-button'
